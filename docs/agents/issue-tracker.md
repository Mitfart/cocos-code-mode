# Issue tracker

Primary tracker: GitHub issues on `Mitfart/cocos-code-mode`.

Remote upstream exists: `RomaRogov/cocos-code-mode`; distinguish local fork fixes from upstreamable changes.

Issue shape:
- problem / expected behavior
- repro steps or target editor workflow
- affected files/tools
- validation: build, package, or CodeMode MCP/editor check
